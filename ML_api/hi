we have api files in this directory
we have to use virtual environment with several libraries
pip install fastapi
pip install uvicorn
pip install pickle5
pip install pydantic
pip install scikit-learn
pip install requests
pip install pypi-json
